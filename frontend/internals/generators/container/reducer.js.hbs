import { DEFAULT_ACTION } from './constants'
import { defaultRoutine } from './actions'

const initialState = {
}

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {

    case DEFAULT_ACTION:
      return state

    case defaultRoutine.TRIGGER:
      return state

    default:
      return state
  }
}

export default {{ camelCase name }}Reducer
