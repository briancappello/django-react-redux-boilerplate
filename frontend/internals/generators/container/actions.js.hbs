import { createRoutine } from 'redux-saga-routines'

import {
  DEFAULT_ACTION,
  DEFAULT_ROUTINE,
} from './constants'

export function defaultAction() {
  return {
    type: DEFAULT_ACTION,
  }
}

export const defaultRoutine = createRoutine(DEFAULT_ROUTINE)
